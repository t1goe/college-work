#lang racket
(define positions(lambda (N A L)(cond
                                  ((null? L) '())
                                  (()())
                                  )
                   )
  )